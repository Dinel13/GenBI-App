class Artikel {
  constructor(id, userId, title, imageUri, isi, komen) {
    this.id = id;
    this.userId = userId;
    this.title = title;
    this.imageUri = imageUri;
    this.isi = isi;
    this.komen = komen;
  }
}

export default Artikel;
