class Event {
  constructor(id, title, imageUri, desc, waktu, alamat, link, komsat) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.desc = desc
    this.waktu = waktu
    this.alamat = alamat;
    this.link = link;
    this.komsat = komsat;
  }
}

export default Event;
